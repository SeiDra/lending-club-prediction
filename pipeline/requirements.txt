# ── Dépendances de l'application (déploiement) ────────────────────────────
# Version allégée du requirements.txt global — uniquement ce dont
# l'app Streamlit a besoin pour tourner en production.

streamlit>=1.35
plotly>=5.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3

# ── Outils CI (format / lint / test) ──────────────────────────────────────
black==24.10.0
pylint
pytest
